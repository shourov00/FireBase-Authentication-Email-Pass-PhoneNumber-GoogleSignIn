<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tutorial.shourov.firebaseauthgooglephoneemailpass">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.main.OptionsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.main.DetailsActivity" />
        <activity android:name=".main.EmailPasswordAuth.EmailPassActivity" />
        <activity android:name=".main.EmailPasswordAuth.SignupActivity" />
        <activity android:name=".main.EmailPasswordAuth.ResetActivity" />
        <activity android:name=".main.PhoneNumberAuth.NumberSigninActivity" />
        <activity android:name=".main.PhoneNumberAuth.SendCodeActivity" />
        <activity android:name=".main.GoogleAuth.GoogleSigninActivity"></activity>
    </application>

</manifest>